import model.Epic;
import model.SubTask;
import model.Task;

public class Main {
    public static void main(String[] args) {
        // Оставил задачи для тестирования

      /*//Тестирование
        //Создаем объекты задач, подзадач, и эпиков

        TaskManager taskManager = new TaskManager();
        Task movement = new Task("Переезд", "Переезжаем в новую квартиру",
                taskManager.newId());
        Task cleaning = new Task ("Уборка", "Убираем квартиру", taskManager.newId());
        Epic vacation = new Epic("Отдых заграницей", "Планирование отдыха",
                taskManager.newId());
        Epic shopping = new Epic("Покупка вещей", "Вещи для отдыха", taskManager.newId());
        SubTask tourBuy = new SubTask("Покупка путевки", "Выбор отеля",
                taskManager.newId(), vacation.getId());
        SubTask collection = new SubTask("Сбор вещей", "Собираем чемоданы",
                taskManager.newId(), vacation.getId());
        SubTask clothes = new SubTask("Поездка в ТЦ", "Покупаем вещи в ТЦ", taskManager.newId(), shopping.getId());

        //Добавляем их в коллекции TaskManager
        taskManager.newTask(movement);
        taskManager.newTask(cleaning);
        taskManager.newEpic(vacation);
        taskManager.newEpic(shopping);
        taskManager.newSubTask(tourBuy);
        taskManager.newSubTask(collection);
        taskManager.newSubTask(clothes);*/

    }
}
